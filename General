-Operaciones logísticas
- 18044-4-2 - Cartilla CTS
- 18044-4-2 - Cartilla de Información cuenta CTS
- REP0170
- 17044-2-6 - Operaciones con PINPADs

[GetMetaRefinanciadoPorMes

ALTER PROCEDURE [Cobranzas].[GetMetaRefinanciadoPorMes]
	@FechaMeta Date
AS    
BEGIN
    SET @FechaMeta = DATEADD(mm, DATEDIFF(mm, 0, @FechaMeta), 0)

	SELECT TOP 1
	    FechaMeta,
        Meta,
        ISNULL(IntervaloInferior, 1) IntervaloInferior,
        ISNULL(IntervaloSuperior, 1) IntervaloSuperior,
        IntervaloSeleccionado
	FROM [Cobranzas].[MetaRefinanciado]
	WHERE FechaMeta = @FechaMeta
END

- Dapper (ORM)
- 18044-4-3 - Validación de reportes Normativos a SBS por SUCAVE


	SQL Server 2008 R2, MongoDB, Web API, RabbitMQ, GoJS, Entity Framework, MVC, Quartz 2.2, C#, Ajax, JQuery, SignalR (WebSocket), Razor v2, Net Framework 4.5, Kentico CMS for ASP.Net, Sencha (Ext JS 5), Bootstrap 3, Jquery Datatable, jqGrid, AutoMapper, Log4net, Postsharp, Xamarin (Movil), Java (Spring), Windows Form.
